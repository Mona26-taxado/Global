# TESTING

```bash
npm test
npm run build
```

Mobile:

A. Android Chrome → Connect → Trust Wallet → approve connection (not send) → address → verify.

B. Android Chrome → Connect → TokenPocket → login/authorize (not transfer) → callback → address.

See TESTING checklist in the product brief (reject, timeout, wrong network, signature, referral, payment mismatches).
