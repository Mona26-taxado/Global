# NETWORK

First empty seat is strict level-order for every `plan_id`: ROOT.LEFT, then ROOT.RIGHT, then row by row LEFT to RIGHT. ACTIVE occupies. HISTORY is ancestry only. Independent of sponsor_id. Paid re-entry uses the same first-empty hole. Direct #2 that completes a parent’s two legs funds that parent’s move.

250 DEMO users are seeded. Labelled DEMO DATA. Prototype Global rules live in `global_config` (admin settings). Not an income guarantee.
