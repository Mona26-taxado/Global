# NETWORK

First empty seat is ROOT-level leg-first: ROOT.LEFT, then that member’s immediate LEFT and RIGHT, then ROOT.RIGHT stays unlocked for that plan tree. Other parents fill LEFT then RIGHT. Not recursive. Same rule for every `plan_id`. Stored in `network_positions`. Independent of sponsor_id. Paid re-entry uses the same first-empty hole.

250 DEMO users are seeded. Labelled DEMO DATA. Prototype Global rules live in `global_config` (admin settings). Not an income guarantee.
