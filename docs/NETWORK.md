# NETWORK

First empty seat is two-phase for every `plan_id`: PHASE 1 ROOT.LEFT, then that member’s immediate LEFT and RIGHT, then ROOT.RIGHT unlocks; PHASE 2 is strict level-order (top to bottom, LEFT to RIGHT) across the opened tree. ACTIVE occupies. HISTORY is ancestry only. Independent of sponsor_id. Paid re-entry uses the same first-empty hole.

250 DEMO users are seeded. Labelled DEMO DATA. Prototype Global rules live in `global_config` (admin settings). Not an income guarantee.
